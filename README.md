# MongoDB Backup Server

Browsable REST API to manage MongoDB collections backups.

## Install

```bash
npm install
```

## Config

Edit the file `.env.example` to include the MongoDB URI and rename it to `.env`.

## Usage

```bash
npm start
```
